

  <!-- <div class="row"> -->
  <div class ="container">
    <div class="row">
    <
      <div class=" col-xs-3">
        <%= render 'books/list', user: current_user , book:@book%>
      </div>
  

    <div class ="pull-left col-xs-9">

        <h1>Books</h1>
      <table>
        <tr>
        <td class="pr-10">Title</td>
        <td>Opinion</td>
        </tr>
      </table>

        <table class="table table-striped">
          <% @books.each do |book| %>
          <tr>
            <td><p>
              <%= link_to user_path(book.user) do %>

                <%= attachment_image_tag book.user, :profile_image, fallback:"snow.jpg",format:'jpeg',size: "30x30"%>


              <%end%>
              </p></td>
            <td><p><%= link_to book.title , book_path(book)%></p></td>
            <td><p><%= book.body%></p></td>
          </tr>

      <%end%>
      </table>

      </div>
      </div>

    <!-- </div> -->
